CREATE TABLE task(
       ID INT AUTO_INCREMENT PRIMARY KEY   
       title VARCHAR(100) NOT NULL,
       status ENUM ('pedente','concluida') DEFAULT 'Pedente',
       created_at DATETIME DEFAULT CURRENT_TIMESTAMP);    
